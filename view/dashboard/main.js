//Load jquery with no conflicts
var $j = jQuery.noConflict();

$j(function(){

    /*
    * query the elements using the noConficts stement
     */
    //$j("#show-secret-key").click(function(){ });

});